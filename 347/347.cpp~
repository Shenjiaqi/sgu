#include <string>
#include <algorithm>
#include <iostream>
#include <vector>
#include <cstdio>
using namespace std;
int main()
{
  freopen("in.txt","r",stdin);
  int n;
  cin>>n;
  vector<string> p;
  p.resize(n);
  for(int i=0;i<n;++i)
    cin>>p[i];
  sort(p.begin(),p.end());
  for(int i=0;i<n;++i)
    cout<<p[i]<<endl;
  return 0;
}
